<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.graduation.yallamana">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".DriverActivity" />
        <activity android:name=".Request_trip" />
        <activity android:name=".Edit_account" />
        <activity android:name=".offer_trip" />
        <activity android:name=".Trip_information" />
        <!--
        activity
            android:name="com.example.m_7el.yallamana.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.graduation.yallamana.MainActivity" />

        </activity><!
        -->
        <activity
            android:name=".Dawer_List"
            android:label="@string/title_activity_dawer__list"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>